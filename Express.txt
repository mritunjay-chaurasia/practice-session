1- How do you secure backend APIs?
    a- JWT authentication
    b- input validation
    c- role-based authorization
    d- rate limiting
    e- secure headers
    f- HTTPS
    g- middleware base validation using libraries like Joi or express-validator

2. Explain how middleware works in Express.js.
    Middleware are a function that runs between the client request and the server response.
    Middleware can do like:
    a- Executes logic
    b- handle authentication and authorization
    c- validate data
    d- handle errors
    e- modified request and response
    f- pass control using next()
    g- log request
Types of Middleware in Express:
a- Application-level middleware
    Defined using app.use() or app.METHOD()

    app.use((req, res, next) => {
    console.log("Time:", Date.now());
    next();
    });
b- Router-level middleware
    Works like app-level but applied only to specific routes.

    const router = express.Router();
    router.use((req, res, next) => {
    console.log("Router middleware");
    next();
    });

c- Built-in middleware (provided by Express)

    express.json() → parses JSON requests
    express.urlencoded() → parses URL-encoded data
    express.static() → serves static files

    app.use(express.json());
    app.use(express.static("public"));

d- Error-handling middleware
    Must have 4 parameters: (err, req, res, next)

    app.use((err, req, res, next) => {
    console.error(err.stack);
    res.status(500).send("Something broke!");
    });

e- Third-party middleware
    Installed via npm.
    Example: morgan (logging), cors (Cross-Origin Resource Sharing).

    const morgan = require("morgan");
    app.use(morgan("dev"));

3. What are the benefits of using TypeScript in Node.js?
    Type safety reduces runtime errors.
    Easier refactoring and maintainability.

4. Advantage of using express.js
    a- Simplified web developement
    b- flexible routing system
    c- Middleware support
    d- Template engine integration

5- Purpose of app.use() used to execute funtion globally
6- Request Pipeline in express :
   It is a series of middleware function that handle incomming HTTP request and pass control to the next function
7- CORS (Cross origin resource sharing): Browser block frontend javascript code from making request from one port to another port 

8- REST APIs => (Representational State Transform) Application programming Interface are architectural style for designing networked application . It is stateless and follow protocol of http or https. format like json and xml
A REST API is called stateless because the server does not store any client session information between requests.
SOAP API => Simplified Object Access protocol and follow protoco http or smtp and have state or stateless 

9- Libuv library: Provides a thread pool and handles background tasks like file system operations and networking.

10- Non-blocking I/O: Lets Node.js process thousands of concurrent requests efficiently without creating multiple threads

 11- What are some popular alternatives to ExpressJS?
    There are several popular alternatives to ExpressJS which includes:
    Koa.js
    Hapi.js
    Sails.js
    Fastify

12- Rate Limiting
Rate limiting is a technique, used to control the number of request a client 

    const express = require('express');
    const rateLimit = require('express-rate-limit');
    const app = express();

    // Create a rate limiter
    const limiter = rateLimit({
    windowMs: 15 * 60 * 1000, // 15 minutes
    max: 100, // Limit each IP to 100 requests per window
    message: 'Too many requests from this IP, please try again later.',
    });

    // Apply to all routes
    app.use(limiter);

    app.get('/', (req, res) => {
    res.send('Welcome to the API');
    });

    app.listen(5000, () => console.log('Server running on port 5000'));

Now each IP can only send 100 requests per 15 minutes.
After exceeding that, the server automatically responds with 429 Too Many Requests

13- Diff between REST api and GraphQL
REST api have multiple endpoint and GraphQL have single endpoint.

14- How do you handle large files or many uploads?
    Chunk uploads
    Background processing
    Limit file size
    Parallel upload control
    Stream files instead of buffering

15- How do you handle errors globally?
    a- proper logging
    b- central error middleware
    c- custom error classes
    d- proper HTTP status code


